pipeline {
  agent any
  stages {
    stage('Build') { steps { echo 'Build from Jenkinsfile' } }
    stage('Test')  { steps { echo 'Test from Jenkinsfile' } }
    stage('Deploy'){ steps { echo 'Deploy from Jenkinsfile' } }
  }
}
